const merchant = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

const merchants = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default merchants;
